{
  "name": "@themg/contextops-datahub-agent",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "demo": "npm run build && node dist/src/cli.js",
    "demo:json": "npm run build && node dist/src/cli.js",
    "test": "npm run build && node --test dist/tests/*.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.3"
  }
}
